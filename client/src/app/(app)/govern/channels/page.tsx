import { PlannedSurface } from "@/components/screens/planned-surface";

export const metadata = { title: "Channels & numbers · Humanoid" };

export default function Page() {
  return <PlannedSurface title="Channels & numbers" />;
}
