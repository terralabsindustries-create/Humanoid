import { PlannedSurface } from "@/components/screens/planned-surface";

export const metadata = { title: "Audit log · Humanoid" };

export default function Page() {
  return <PlannedSurface title="Audit log" />;
}
